{% include 'header.html' %}

{% include 'navbar.html' %}

<div class="container" style="width: 75%">
    <table class="table">
        <th>Title</th>

        <th>Author</th>

        <th>Release year</th>

        <th>ISBN-10</th>

        <th>Cover</th>

        {% for book in names %}
            <tr>
                <td> 
                    {{ book.title }} 
                </td>

                <td>
                    {{book.author}}
                </td>

                <td>
                    {{ book.release_year }}
                </td>
                
                <td> 
                    {{ book.isbn10 }} 
                </td>

                <td>
                    <a href="borrowbook?title={{book.title}}"><img src="http://covers.openlibrary.org/b/isbn/{{ book.isbn10 }}-M.jpg"></a>
                </td>
            </tr>
        {% else %}
            <p>
                Fail :|
            </p>
        {% endfor %}
        </tr>
    </table>
</div>

{% include 'footer.html' %}

ON Books_genre.bookid = books.id AND 